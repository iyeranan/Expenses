<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.expenses"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="3" />

    <application android:icon="@drawable/expenses" android:label="@string/app_name" android:debuggable="true">
        <activity android:name=".ExpensesActivity"
                  android:label="@string/app_name" android:windowSoftInputMode="stateAlwaysVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExpensesShareActivity" android:label="@string/ExpensesShare" android:windowSoftInputMode="stateAlwaysVisible">
        </activity>
        <activity android:name="NetExpensesActivity" android:label="@string/netExpenses" android:windowSoftInputMode="stateAlwaysVisible"></activity>

    </application>
</manifest>